{
  "name": "dreamate-canvas-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node preview-server.js",
    "check": "node --check app.js && node --check preview-server.js && node --check api/index.js && node --check lib/api-handler.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/blob": "^1.0.2"
  }
}
