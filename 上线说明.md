# 上线说明

## 已适配的线上存储

这一版本地仍然可以用 `preview-server.js` 预览；部署到 Vercel 后，`api/[...path].js` 会接管接口，并使用 Vercel Blob 持久化：

- 默认画布：`data/default-canvas.json`
- 点赞数：`data/likes.json`
- 留言：`data/messages.json`
- HTML 附件索引：`data/html-assets.json`
- 管理员上传图片：`uploads/*`
- 管理员上传 HTML：`html-pages/*`
- 管理员替换 PDF：`pdf/resume.pdf`、`pdf/portfolio.pdf`

如果 Blob 里还没有数据，接口会先读取仓库中现有的 `data/` 和 `assets/` 文件作为初始数据，所以当前画布内容会被保留。

## Vercel 环境变量

在 Vercel 项目里配置：

- `ADMIN_PASSWORD`：管理员密码
- `ADMIN_SESSION_SECRET`：一段随机长字符串
- `BLOB_READ_WRITE_TOKEN`：Vercel Blob 自动提供或手动绑定后的读写 token
- `BLOB_PREFIX`：可选，默认 `dreamate`

不要把真实密码写进 GitHub 仓库。

## GitHub 上传

如果当前目录还不是 Git 仓库：

```bash
git init
git add .
git commit -m "Prepare Vercel deployment"
git branch -M main
git remote add origin 你的仓库地址
git push -u origin main
```

## Vercel 部署

1. 在 Vercel 里 Import Git Repository。
2. Framework Preset 选择 `Other`。
3. Build Command 留空或使用默认。
4. Output Directory 留空。
5. 配好上面的环境变量。
6. 绑定 Vercel Blob Store。
7. Deploy。

## 注意

Vercel Functions 不适合走超大 base64 上传。当前 PDF 初始文件会正常作为静态资源/接口 fallback 使用；如果未来经常在后台替换很大的 PDF，建议再升级成 Vercel Blob 客户端直传。
