# 个人无限画布求职网站 Demo 初始前置计划

> 本计划用于后续窗口继续实现项目时快速理解整体目标、设计来源、交互规则与实现边界。当前阶段只做本地独立网页 demo，不接入后台，不做持久化画布编辑。

## 1. 项目目标

制作一个用于求职展示的个人网站 demo，形式是类似 Figma 的无限画布。访客进入后可以浏览个人介绍、作品占位模块，并临时拖拽、缩放、连线、添加内容块。所有画布编辑只在当前访问中生效，刷新后恢复默认状态。

最终 demo 形态：

- 独立网页，本地打开或本地服务运行。
- 桌面端优先，基于 1440 x 900 Figma 设计稿。
- 保留基础响应式能力，但暂不做移动端专项适配。
- 使用原生 HTML / CSS / JS，不引入前端框架。
- 后续具体内容、文案和模块设计会继续由 Figma 文件补充。

## 2. 设计来源

Figma 文件：

- 文件 key：`AXHcdv6zwc7GHEtuKd4vSw`
- Section：`107:283`
- 加载页：`2:156`
- 主画布页：`1:3`
- 顶部栏：`14:180`
- 底部工具栏：`57:45`
- 点赞按钮状态：`94:244`
- 树状开关变体：`72:143`
- 选择 / 抓手工具变体：`103:278`

设计基准：

- 主画布尺寸：`1440 x 900`
- 主蓝：`#385ADF`
- 主绿：`#00FD61`
- 工具选中绿：`#00D00E`
- 画布背景：`#EDEDF3`
- 点阵：单点 `2 x 2px`，间距 `16px`
- 毛玻璃面板：`rgba(255,255,255,0.7)`，顶部和侧栏 blur `10px`，底部工具栏 blur `5px`

已读取到的关键 Figma 备注：

- 加载页是进入个人主页的首屏，确认进入主页后丝滑消失。
- 加载页标题文字使用路径动画，按顺序书写。
- 加载页副文案使用打字机动效，颜色平滑出现，下划线至少闪烁三下。
- 加载完成后，副文案变成“点击任意按键进入首页”，并有 2 秒一次的扫光动效。
- 画布背景是代码实现的点阵，不直接使用设计稿点阵图层。
- 图片块、卡片位置与尺寸需要被点阵约束和吸附。
- Page 加号点击可新建页面，默认名称为 `New page`。
- 当前页面选中态为蓝底白字。
- `作品探索` 当前展示的是未选 + hover 状态，背景为浅灰。
- 树状展开按钮只在 hover 时出现，目前只有 `作品探索` 有树状结构。
- 顶部 `个人画布` tab 适应性居中。
- `联系我` tab 暂不做。
- 顶部加载条只在画布内容加载时出现。
- 点赞后按钮变绿并显示赞量，每次访问只能赞一次。
- 留言按钮点击后未来出现弹窗输入留言，但本轮先不做弹窗。
- 选择模式、抓手模式有未选、悬浮、选中变体。
- 添加按钮 hover 有 `#EEF0F8` 背景，点击展开列表，包含便利贴、图片、文本框、链接卡。
- 连线按钮选中后变绿，进入连线模式。
- 缩放模式选中后滚轮直接缩放；未选中时 `Ctrl/Command + 滚轮` 缩放。
- 内容适应按钮点击后归位到能看见所有内容的状态，边缘留白，过渡丝滑。
- 横向 / 纵向滚动条仅需要时展示。
- Minimap 显示画布内容和当前视图缩放可见范围。
- 连接线逻辑：三等分路径，中间两个点圆角为 `100px`，线宽 `4px` 居中。
- 连接点普通状态下 hover 卡片才显示；连线模式下所有卡片连接点常显。

## 3. 文件结构建议

```text
/
  index.html
  styles.css
  app.js
  万物起源.md
  fonts/
  assets/
    figma/
```

字体资源已确认存在：

```text
fonts/Caveat-Bold.ttf
fonts/Caveat-Regular.ttf
fonts/寒蝉手拙体.ttf
fonts/PingFang SC/pingfangsc-regular.otf
fonts/PingFang SC/pingfangsc-medium.otf
fonts/PingFang SC/pingfangsc-light.otf
fonts/PingFang SC/pingfangsc-semibold.otf
fonts/PingFang SC/pingfangsc-thin.otf
fonts/PingFang SC/pingfangsc-ultralight.otf
fonts/微软雅黑/msyh.ttc
fonts/微软雅黑/msyhbd.ttc
fonts/微软雅黑/msyhl.ttc
```

字体声明建议：

```css
@font-face {
  font-family: "Caveat";
  src: url("./fonts/Caveat-Regular.ttf");
  font-weight: 400;
}

@font-face {
  font-family: "Caveat";
  src: url("./fonts/Caveat-Bold.ttf");
  font-weight: 700;
}

@font-face {
  font-family: "HCSZT";
  src: url("./fonts/寒蝉手拙体.ttf");
}

@font-face {
  font-family: "PingFang SC Local";
  src: url("./fonts/PingFang SC/pingfangsc-regular.otf");
  font-weight: 400;
}

@font-face {
  font-family: "PingFang SC Local";
  src: url("./fonts/PingFang SC/pingfangsc-medium.otf");
  font-weight: 500;
}

@font-face {
  font-family: "PingFang SC Local";
  src: url("./fonts/PingFang SC/pingfangsc-light.otf");
  font-weight: 300;
}
```

UI 字体栈：

```css
font-family: "PingFang SC Local", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
```

## 4. 页面流程

### 加载页

访问页面后先进入加载页。

加载页表现：

- 背景色 `#385ADF`。
- 中央标题：`It‘s Dreamate . 翊涛`。
- 英文使用 Caveat Bold，中文使用寒蝉手拙体。
- 标题需要有路径书写感动画。
- 下方文案初始为：`足迹拓印处，是未完的序章_`。
- 文案使用打字机动效出现，颜色平滑显现。
- 末尾下划线至少闪烁三次。
- 加载完成后，文案变成：`点击任意按键进入首页`。
- 新文案带扫光动效，约每 2 秒扫一次。
- 用户点击或按任意键后，加载页淡出，进入主画布。

### 主画布

主画布包含：

- 顶部栏
- 左侧 Page 导航
- 左下 Minimap
- 无限画布区域
- 底部工具栏
- 必要时显示横向 / 纵向滚动条

`联系我` tab 暂不可点。留言按钮暂不弹窗。

## 5. 页面与导航

页面结构：

- `初遇`：真实默认画布。
- `作品探索`：一级分组，不是页面，不可跳转。
- `网易知数`：作品探索子页面，先为空白画布。
- `金保通`：作品探索子页面，先为空白画布。
- `钱包-加油频道`：作品探索子页面，先为空白画布。
- `沉淀小记`：页面，先为空白画布。

Page 规则：

- 当前页面蓝底白字。
- 未选页面为灰蓝文字。
- 未选 hover 背景：`rgba(28,59,180,0.05)`。
- `作品探索` hover 时显示树状展开按钮。
- 树状按钮支持展开 / 收起。
- 一级分组 `作品探索` 不可跳转，只负责展开下级页面。
- 下级页面可跳转到对应空白画布。
- 点击 Page 区加号可新增 `New page`，仅当前访问有效，刷新恢复。

## 6. 默认画布内容

`初遇` 页面默认内容：

- 头像图片卡：
  - 参考 Figma 节点 `14:151`
  - 默认位置约 `x=403, y=234`
  - 尺寸 `200 x 326`
  - 圆角 `12px`
  - 白色 `4px` 边框
  - 阴影 `0 8px 16px rgba(120,136,191,0.25)`

- 白色占位卡：
  - 参考 Figma 节点 `96:245`
  - 默认位置约 `x=830, y=353`
  - 尺寸 `250 x 326`
  - 浅蓝虚线边界

- 默认连接线：
  - 从头像卡右中点连接到白色卡左中点
  - 绿色虚线
  - 线宽 `4px`
  - 圆角曲线路径
  - 跟随模块移动和 resize 自动更新

可额外加入少量求职 / 作品集参考占位模块，但文案只作为 demo 占位，后续会被设计稿替换。

## 7. 画布交互

### 坐标系统

维护虚拟画布坐标：

```js
{
  panX,
  panY,
  scale
}
```

模块数据使用画布坐标：

```js
{
  id,
  type,
  x,
  y,
  width,
  height,
  content
}
```

所有拖拽、缩放、连线、minimap 都基于统一坐标转换。

### 点阵吸附

- 网格间距固定 `16px`。
- 模块移动后，`x/y` 吸附到最近网格点。
- 模块 resize 后，`width/height` 吸附到 `16px` 倍数。
- 吸附影响画布坐标，不影响页面容器布局。

建议最小尺寸：

- 便利贴：`160 x 120`
- 文本框：`160 x 80`
- 图片：`96 x 96`
- 链接卡：`220 x 120`

### 模块选择、拖拽、尺寸调整

- 默认模式为选择模式。
- 点击模块后选中。
- 选中模块显示边框、resize 控件、连接点。
- 拖拽模块主体移动模块。
- 拖拽边缘 / 四角控件调整尺寸，接近 Figma 的 resize 体验。
- 刷新页面后恢复默认状态。

## 8. 工具模式

工具模式互斥：

- `select`
- `hand`
- `connect`
- `zoom`

规则：

- 切换任一模式时取消其他模式选中态。
- 选择模式：选中、移动、resize 模块。
- 抓手模式：按住鼠标左键拖动画布。
- 非抓手模式也支持鼠标中键拖动画布。
- 非抓手模式也支持按住空格 + 鼠标左键拖动画布。
- 连线模式：所有模块连接点常显，可创建连线。
- 缩放模式：滚轮直接缩放。
- 非缩放模式下，`Ctrl + 滚轮` 缩放，mac 支持 `Command + 滚轮`。

## 9. 添加模块

点击添加按钮展开列表，包含：

- 便利贴
- 图片
- 文本框
- 链接卡

新增规则：

- 新模块出现在当前视口中心附近。
- 初始位置吸附到点阵。
- 使用默认占位内容。
- 添加操作进入撤回栈。
- 刷新后新增模块消失。

## 10. 连线

连接点显示规则：

- 普通模式下，hover 模块时显示四条边中点连接点。
- 连线模式下，所有模块连接点常显。

连线创建方式需要都支持：

- 从一个连接点拖到另一个连接点。
- 先点击起点，再点击终点。

连线样式：

- 绿色虚线
- 线宽 `4px`
- 圆角路径
- 跟随模块移动和 resize 自动更新

无效连线取消，不进入状态。

## 11. 撤回

撤回按钮需要真实可用。

至少支持撤回：

- 添加模块
- 移动模块
- resize 模块
- 创建连线
- 删除模块或连线，如果实现删除功能

撤回不作用于：

- 点赞
- 页面切换
- 加载页进入
- 联系我 tab
- 留言按钮

本轮只需要 undo，不需要 redo。

## 12. 缩放与内容适应

缩放：

- scale 范围建议 `0.25` 到 `2.5`。
- 鼠标滚轮缩放时，以鼠标所在位置为缩放中心。
- 工具按钮触发时，可以视口中心为缩放中心。

内容适应：

- 点击内容适应按钮后，计算当前页面所有模块和连接线包围盒。
- 自动调整 pan 和 scale，让全部内容可见。
- 边缘留白约 `64px`。
- 使用 `200-300ms ease` 过渡。
- 空白页面归位到默认 scale `1` 和默认 pan。

## 13. Minimap

Minimap 需要实时展示：

- 当前页面模块简化矩形。
- 当前视口范围。
- 视口框用虚线或半透明描边。
- pan / zoom / 模块变化时实时更新。

可选增强：

- 拖拽 minimap 视口框移动主画布。
- 点击 minimap 某处移动主画布中心。

## 14. 顶部栏功能

### 点赞

初始状态：

- 使用 Figma 未点赞态。
- 默认点赞数 `1234`。

点击第一次点赞：

- 视觉变为已点赞。
- 显示点赞数。
- 点赞数增加一次。
- 记录当前浏览器已经贡献过点赞。

取消点赞：

- 视觉回到未点赞。
- 点赞数不减少。
- 再次点赞不重复增加总数。

本地记录：

```js
localStorage.setItem("portfolioLikeCount", count);
localStorage.setItem("portfolioLikeContributed", "true");
localStorage.setItem("portfolioLikeActive", "true" | "false");
```

### 留言

- 按钮保留。
- 本轮不做弹窗。
- 点击可以无反应，只保留 hover / active 视觉反馈。

### 联系我

- 保留 tab。
- 不可点击。
- 不跳转。

## 15. 资源要求

- Figma 中的头像、图标、按钮图形需要下载到 `assets/figma/`。
- 最终代码引用本地资源。
- 不引用短期 Figma asset URL。
- 如果某个资源暂时无法下载，可以临时替代，但要在实现说明中标记为待替换。

## 16. 实施顺序

1. 建立 `index.html`、`styles.css`、`app.js` 基础结构。
2. 接入本地字体。
3. 下载并整理 Figma 资源。
4. 实现加载页视觉和动画。
5. 实现进入主画布流程。
6. 实现顶部栏、左侧栏、Minimap、底部工具栏静态布局。
7. 实现画布坐标系统、点阵背景、pan / zoom。
8. 实现默认模块渲染。
9. 实现选择、拖拽、resize、点阵吸附。
10. 实现连接点和连线。
11. 实现添加模块菜单。
12. 实现 undo。
13. 实现 Page 切换和树状展开。
14. 实现点赞 localStorage 记录。
15. 实现实时 Minimap。
16. 做桌面端视觉和交互验收。

## 17. 验收标准

- 打开页面后先进入加载页。
- 加载页有书写动画、打字机动画、下划线闪烁、扫光提示。
- 用户手动点击或按键后进入主画布。
- 主画布布局接近 Figma。
- 点阵背景按 `2px / 16px` 规则生成。
- 默认模块、连接线位置接近设计稿。
- 模块可选择、拖拽、resize。
- 移动和 resize 后吸附点阵。
- hover 模块显示连接点。
- 连线模式下所有连接点常显。
- 可创建连接线，连接线跟随模块更新。
- 可添加四类占位模块。
- 撤回可用。
- 工具模式互斥。
- 抓手、缩放、内容适应功能可用。
- Minimap 实时反映模块和视口。
- 页面导航可切换，作品探索一级分组不可跳转。
- 点赞只在首次贡献时增加计数。
- 刷新后画布编辑恢复默认。
- 留言不弹窗。
- 联系我不可点。
- 控制台无明显报错。
- 页面不依赖 Figma 临时资源 URL。

## 18. 暂不做内容

- 管理员后台。
- 云端保存。
- 画布编辑持久化。
- 留言弹窗。
- 联系我页面。
- 移动端专项适配。
- redo。
- 多人协作。
- 权限系统。
