# 个人画布式网站交接文档：雏形初具

> 给新窗口 / 新会话接手用。本文结合 `万物起源.md` 与当前会话已经完成的实现结果整理，只记录当前目标、现状、约定、注意事项和常用实现方法，不记录过程争论。

## 1. 项目定位

这是一个本地独立运行的个人求职展示网站 demo，核心体验是「类似 Figma 的个人无限画布」。

当前阶段目标：

- 使用原生 `HTML / CSS / JS`，不引入 Vue、React 等框架。
- 页面从加载页进入主画布。
- 主画布支持页面导航、卡片拖拽、尺寸调整、连线、添加内容块、编组、复制粘贴、撤销、缩放、适应预览、Minimap。
- 所有画布编辑只在当前运行期间生效，刷新后默认内容恢复；点赞、剪切板等少量状态使用 `localStorage`。
- 优先桌面端，基于 1440 x 900 Figma 设计稿。

当前主要文件：

```text
E:\Vibe_coding\个人web_画布式\index.html
E:\Vibe_coding\个人web_画布式\styles.css
E:\Vibe_coding\个人web_画布式\app.js
E:\Vibe_coding\个人web_画布式\preview-server.js
E:\Vibe_coding\个人web_画布式\万物起源.md
E:\Vibe_coding\个人web_画布式\雏形初具.md
```

重要资源目录：

```text
assets/figma/
assets/cursors-normal/
assets/cursors-small/
assets/lottie.min.js
assets/loading-title-animation-data.js
assets/加载页文字路径动画.json
assets/指针 light/
fonts/
```

本地预览：

```powershell
node preview-server.js
```

默认端口为 `4173`，访问：

```text
http://127.0.0.1:4173/index.html
```

## 2. 设计来源与全局视觉约定

Figma 文件：

```text
https://www.figma.com/design/AXHcdv6zwc7GHEtuKd4vSw/个人站设计稿
```

常用节点曾多次参考：

- 加载页 / 主画布：`107:283`
- Page 栏相关：`151:1005`、`126:284`、`129:327`
- 顶部点赞 / 留言：`94:244`、`94:238`、`94:241`、`47:346`
- 底部工具栏：`157:1026`、`55:62`、`103:278`、`75:194`
- 卡片选中边缘 / 操作按钮 / 小蓝环：`247:49`

全局颜色：

```css
--blue: #385adf;
--green: #00fd61;
--tool-green: #00d00e;
--canvas: #ededf3;
--text: #454e70;
--muted: #5e678d;
--glass: rgba(255, 255, 255, 0.7);
```

当前关键视觉约定：

- 顶部栏填充：`rgba(255, 255, 255, 0.7)`，背景模糊 `10px`。
- Page 栏和 Minimap 面板背景模糊：`20px`。
- 所有普通卡片背景都有约 `10px` 背景模糊。
- 编组卡片自身背景：`70%` 纯白，背景模糊 `20px`。
- 画布背景点阵：代码生成，不用图片；单点当前为 `3 x 3px`，单点大小不随视图缩放，缩放只改变点阵间距的视觉感。
- 连接线默认线宽 `2px`，线宽不随画布缩放变粗变细。
- 卡片功能按钮、连接点、下拉菜单等操作控件要保持 100% 视角下的屏幕尺寸，不随画布缩放变大变小；位置跟随卡片。

## 3. 加载页当前状态

加载页已经从普通标题改为 Lottie 路径书写动画。

当前结构：

- `index.html` 加载：
  - `assets/lottie.min.js`
  - `assets/loading-title-animation-data.js`
  - `app.js`
- `assets/loading-title-animation-data.js` 将 `assets/加载页文字路径动画.json` 内嵌到 `window.LOADING_TITLE_ANIMATION_DATA`。
- 这样可以避免直接用 `file://` 加载 JSON 失败。

当前行为：

- 背景色为主题蓝。
- 标题使用 Lottie 路径动画，只播放一次并停留最后一帧。
- 原来的“足迹拓印处，是未完的序章_”及打字动画已移除。
- 只保留提示文案：`点击任意按键进入首页`。
- 提示文案在 Lottie 播放到约 90% 时渐显。
- 提示文字保留扫光动效。
- 用户点击或按键后，加载页淡出，进入主画布。

加载页注意事项：

- 不要再直接 `fetch()` Lottie JSON，否则本地文件打开时容易空白。
- Lottie 尺寸在样式中被放大过，若重新调整请同时看 `loading-stack`、`loading-title`、`loading-copy` 的位置关系。
- 扫光此前反复调整过：要求是首尾相接、方向正确、速度约为之前的 1.5 倍，不要改回卡顿或块状扫光。

## 4. 主画布整体结构

主画布包含：

- 顶部栏：品牌、顶部 Tab、点赞按钮、留言按钮、顶部加载条。
- 左侧 Page 栏：页面树、新增页面、删除页面、双击编辑页面名。
- 左下 Minimap：当前内容缩略图、视图框、缩放数字。
- 画布区域：无限画布、点阵背景、卡片、连接线。
- 底部工具栏：选择 / 抓手、添加、连线、撤销、缩放、适应预览等。
- 自定义滚动条：仅必要时出现，容器边距已按右/下内边距要求调整过。

核心状态变量在 `app.js`：

```js
pages
activePage
blocks
connections
selectedIds
panX
panY
scale
tool
dragState
undoStack
fitScale
```

坐标系统：

- 卡片数据使用画布坐标。
- 屏幕坐标与画布坐标通过 `screenToCanvas()` / `canvasToScreen()` 转换。
- 画布世界层和连接层同步应用：

```js
translate(panX, panY) scale(scale)
```

## 5. Page 栏现状

默认页面结构：

- `初遇`
- `作品探索`：一级分组，不是页面。
- `网易知数`
- `金保通`
- `钱包-加油频道`
- `沉淀小记`

当前规则：

- Page 当前态为蓝底白字。
- Page hover 有浅蓝背景。
- `作品探索` 可展开 / 收起；树状展开按钮只有 hover / focus 时出现。
- 默认页面删除时有二次确认：
  - “不再多了解我一些吗？稍后可以撤销删除”
  - “手滑了”
  - “我很确定”
- 自建页面删除不需要二次确认。
- 删除 Page 可撤销。
- Page 名称支持双击编辑。
- Page 多了以后，左侧 Page 列表支持内部滚动；标题区固定不被挤走。

注意：

- `作品探索` 本身不可跳转，只负责展开树。
- 当前 Page 新增名为 `New page`。
- Page 删除、添加、重命名要进入撤销栈。

## 6. 卡片系统当前规则

卡片类型：

- 头像图片卡
- 占位卡
- 便利贴
- 图片块
- 文本框
- 链接卡
- 编组卡片

卡片通用行为：

- 点击选中。
- 拖拽卡片空白区域移动卡片。
- 拖拽边缘 / 四角调整尺寸。
- 移动和尺寸调整会吸附点阵。
- 支持撤销。
- hover 卡片整体时，卡片右侧上方外部显示功能按钮。
- 卡片功能按钮采用 Figma 素材图标，不要随手用文字或 CSS 画。

当前选中边缘：

- 选中后卡片描边为 `1px` 主题蓝。
- 四角为从 Figma 提取的蓝色三分之一环形元素。
- 四角小环是 resize 热区。
- 四角小环尺寸随画布缩放一起变化，位置要贴住卡片边缘。
- 连接点不属于小环，不要混用。

重要注意：

- 父级被选中时，内部卡片不应该显示选中态、缩放点、连接点、操作按钮。
- CSS 相关选择器要使用直属子级写法，例如：

```css
.block.selected > .resize-handle
.block.selected > .connection-point
.block.selected > .card-actions
```

避免写成普通后代选择器，否则编组内子卡片会被父级选中态误伤。

## 7. 卡片内容编辑

卡片内部元素包括：

- 标题
- 正文
- 图片
- 链接文本

内容交互：

- 所有文字内容可编辑。
- hover 到文字内容时出现虚线框。
- 点击文字可编辑。
- 正文支持回车换行。
- 标题禁止回车换行。
- 正文字号当前为 `14px`，标题为 `20px`。
- hover 图片元素时显示“替换图片”，同时保留红色叉叉删除元素。
- 图片元素不可被浏览器原生拖出打开新标签，相关 `draggable` 和 `-webkit-user-drag` 已处理。

卡片内部元素拖拽规则：

- 如果卡片只有单个元素，按住卡片任意位置移动卡片。
- 如果卡片有多个元素，按住空白区域移动卡片，按住元素区域调整元素顺序。
- 元素顺序调整需要有顺滑位移动画。
- 不要让元素内容被拖出卡片形成浏览器原生拖拽。

图片块特殊规则：

- 新建图片块不带标题元素。
- 图片块只能添加图片元素。
- 图片块空状态显示“待插入图片”。
- 图片块可插入多张图片。
- 图片元素默认撑满可用空间，但不能超出图片块尺寸；超出部分裁切。
- 点击“适应”时，使用当前图片尺寸重新计算卡片，显示完整图片与合理间距，不应回到丢失的原始尺寸状态。

## 8. 卡片自适应与自定义尺寸

卡片有两种尺寸模式：

### 适应模式

- 未手动拖拽尺寸时，卡片处于适应模式。
- 卡片宽高根据内部元素数量、尺寸、排列方向自动变化。
- 内边距为 `16px`。
- 元素间距为 `8px`。
- 文本、图片都要参与尺寸计算。
- 切换排列后自动进入适应。

### 自定义宽高模式

- 卡片尺寸被手动拖拽后，进入自定义宽高模式。
- 内部元素随卡片尺寸变化。
- 自定义尺寸下，卡片右侧功能按钮中出现“适应”按钮。
- 点击“适应”后：
  - 内部元素保持当前尺寸。
  - 卡片按照适应规则重新包裹。
  - 解决留白、裁切、重叠。

注意：

- 拖拽卡片尺寸不能低于内部元素的合理最小尺寸，否则文字 / 图片会重叠。
- 文本类卡片曾多次出现底部文字被裁切，需要同时考虑 `scrollHeight`、实际 DOM 渲染高度、元素间距和卡片内边距。

## 9. 编组当前状态

编组行为是当前项目最容易出错的区域，接手时请重点阅读。

当前编组规则：

- 多选卡片后出现多选操作按钮：删除、编组。
- 编组不是生成“预览副本”，而是真实父子结构。
- 被编组的原卡片会成为编组的子卡片，并从顶层渲染中移除。
- 编组卡片包含真实的内部卡片，内部卡片仍可编辑、移动、连线。
- 编组卡片自身有 70% 白色背景和 `20px` 背景模糊。
- 编组创建初始状态下，内部元素距离编组卡片边距应为约 `16px`。
- 16px 只是创建初始规则，后续可以正常拖拽编组尺寸。
- 编组手动拖拽尺寸后，尊重自定义尺寸，不要强制回到 16px 包裹。

当前实现思路：

- `parentGroup` 表示卡片属于某个编组。
- 顶层渲染用 `topLevelBlocks()`，过滤掉 `parentGroup` 的子卡片。
- 编组渲染时通过 `childBlocks(group.id)` 把子卡片作为 DOM 子节点插入。
- `getBlockAbsolutePosition()` 用于计算嵌套卡片的绝对画布坐标。
- 连线、Minimap、选区等涉及位置时，需要明确使用绝对坐标或顶层坐标。

编组尺寸校准：

- `normalizeGroupSize()` / `normalizeGroupSizes()` 负责让未自定义尺寸的编组包住子卡片。
- `calibrateRenderedGroupSizes()` 用真实 DOM 尺寸二次校准，避免文本 / 图片实际渲染后撑破外框。
- 拖动内部子卡片时，必须暂停对应父编组的自动适应，否则会出现“内部卡片移动速度时快时慢、不跟随鼠标”。
- 松手后再重新校准父编组外框。

编组菜单：

- 删除：删除整个编组及内部子卡片。
- 置于顶层。
- 取消编组：子卡片恢复为顶层卡片，位置保持视觉不变。

已修复过的问题，别改回去：

- 编组不能只是额外生成一个空外框预览，否则内部不会跟着移动，还会在原处留下残影。
- 父级被选中时，内部子卡片不要显示选中蓝边。
- 编组内连接线不要被编组背景盖住。
- 连接线层级目前高于卡片世界层，`pointer-events: none`，避免挡住交互。

## 10. 连线系统当前规则

连接点：

- 普通模式下，hover 或选中卡片显示直属连接点。
- 连线模式下，连接点常显。
- 连接点视觉圆点为 `16px`。
- 连接点吸附热区扩大到 `24px`，通过透明外层 + `::after` 小圆点实现。
- 连接点和线宽不随画布缩放视觉变大变小。

连线规则：

- 支持从连接点拖出预览线。
- 连接到第二个连接点时，预览线应变成最终连接线的形态。
- 未连接到第二点时，末端接近鼠标位置，末端不再一直朝左。
- 完成连接后有虚线滚动动画，滚动时间较长，速度曲线是开始快、结尾慢。
- 之前尝试过颜色流光，最终去掉，因为效果不好。
- 虚线间距已经缩小过。
- 线宽默认 `2px`。

编组内外连线规则：

- 顶层卡片之间可以连线。
- 同一个编组内部的子卡片可以互相连线。
- 编组内部卡片不能与编组外部卡片相连。
- 不同编组内部卡片不能直接相连。

技术注意：

- `getConnectionTarget()` 基于 `elementFromPoint(...).closest(".connection-point")` 找目标。
- `canConnectBlocks(fromBlock, toBlock)` 负责限制编组内外边界。
- `renderConnections()` 也要用 `canConnectBlocks()` 过滤非法旧线。
- `getAnchor()` 必须使用 `getBlockAbsolutePosition()`，否则编组内卡片连接线位置会错。

## 11. 顶部点赞按钮

当前点赞按钮重点：

- 默认必须是未点赞状态。
- 默认数字为 `1234`，点赞后为 `1235`。
- 爱心图标来自 Figma `18 x 18px` 图标，不要拉伸。
- 未点赞 / 已点赞分别使用 Figma 提取资源。
- 点赞后容器颜色不变，爱心变绿色。
- 点赞动画包含：
  - 色块 / 容器有柔和拉伸变化。
  - 爱心缩放一下后保持。
  - 数字有滚动效果。

数字滚动规则：

- 从旧数字滚到新数字。
- 位数按个、十、百、千、万顺序依次滚动。
- 前一位滚动到约 75% 时，下一位开始。
- 如果位数增加，例如 `999 -> 1000`，新增的最高位等前面数字滚完后再丝滑出现。
- 整个动画时长约 `0.8s`。
- 按钮宽度要随数字宽度丝滑变化，但数字本身不能横向抽搐。
- 不要在动画结束时整段重建 DOM，否则最后一刻会跳一下。

本地存储键已多次迭代，当前代码里使用的是较新的版本键，接手时直接看 `initLike()`。

## 12. 复制粘贴卡片

当前支持：

- `Ctrl/Cmd + C` 复制选中卡片。
- `Ctrl/Cmd + V` 粘贴卡片。
- 不显示按钮。
- 可跨 Page 粘贴。
- 同一 Page 粘贴时偏移一点，避免覆盖原对象。
- 跨 Page 粘贴时放到当前视野中心附近。
- 复制编组时保留编组结构、内部子卡片和这些卡片之间的内部连线。

重要：不要再读取系统剪切板。

原因：

- `navigator.clipboard.readText()` 会触发浏览器权限弹窗：“此文件想要查看复制到剪贴板的文本和图像”。
- 用户明确要求不要显示。

当前实现：

- 复制时尝试写 `navigator.clipboard.writeText()`，这通常不会弹读权限。
- 同时把应用内剪切板写入 `cardClipboard`。
- 也写入 `localStorage.dreamateCardClipboard`，用于同站点持久兜底。
- 粘贴时只读 `cardClipboard` / `localStorage`，不调用 `navigator.clipboard.readText()`。

复制粘贴注意：

- 粘贴时必须重新生成所有卡片 ID、元素 ID、连接线 ID。
- 编组子卡片的 `parentGroup` 要映射到新的编组 ID。
- 如果只复制了编组内单个子卡片，没有复制父编组，应把它转为顶层卡片，并使用视觉绝对坐标。

## 13. 鼠标指针

用户提供了指针资源：

```text
assets/指针 light/
```

当前实际使用：

```text
assets/cursors-normal/
```

原因：

- 原始 `.cur` 内含 `32 / 48 / 64 / 96` 多尺寸，浏览器会自行挑选，容易显示过大。
- 曾尝试 `16 x 16` 半尺寸，用户反馈太小。
- 当前固定为 `32 x 32` PNG 正常尺寸。

已覆盖：

- 默认指针
- 可点击指针
- 文本编辑指针
- 移动 / 拖拽指针
- 横向 / 纵向缩放
- 两条对角线缩放
- 画布最左 / 最右 / 滚动条 / 底部栏等边缘区域，避免掉回系统指针。

注意：

- 对角线缩放分别使用 `dgn1.png` / `dgn2.png`。
- 光标热点坐标在 CSS 变量中声明。

## 14. 底部工具栏与操作按钮

底部工具栏：

- 选择 / 抓手模式按钮组合需要遵循 Figma 间距。
- 下拉箭头容器为 `12 x 24px`。
- 箭头与左侧图标间距 `4px`。
- 这组整体再与其他按钮相隔 `16px`。
- 底部小字不要闪烁，不要折行。

卡片功能按钮：

- hover / 选中卡片时，卡片右侧上方外部出现图标按钮。
- 当前按钮来自 Figma 资源，不要用文本替代。
- 删除按钮为红色背景，删除相关文字色 `#CC073F`。
- 更多菜单含：
  - 普通卡片：置于顶部、添加标题、添加文本、添加图片、切换布局。
  - 图片卡片：置于顶部、添加图片、切换布局。
  - 编组：删除、置于顶层、取消编组。
- 自定义尺寸模式下，删除和更多左侧出现“适应”按钮。

## 15. Minimap、缩放与滚动条

Minimap：

- 显示当前页面内容缩略矩形。
- 右侧显示当前缩放数值，如 `Now: 100%`。
- 点击 Minimap / 内容适应按钮可以触发最佳预览。
- 最佳预览应有合理留白，不要让元素顶边。
- 最佳预览临界状态下，Minimap 不应显示额外虚线框。

滚动条：

- 仅在需要时出现。
- 点击最佳预览后不应出现滚动条。
- 只有在最佳预览状态继续放大，才出现滚动条。
- 滚动条容器边距：右 / 下内边距为 `8px`，其余为 `2px`。
- 右下角不应重叠。

缩放：

- 画布内容随 `scale` 缩放。
- 操作 UI 如卡片按钮、连接点、菜单、线宽使用反向缩放或 CSS 变量保持视觉尺寸。
- 点阵单点大小不随缩放变化；缩放只影响点阵间距。

## 16. 撤销

撤销栈使用 `snapshot()` 保存：

- pages
- activePage
- blocks
- connections
- panX
- panY
- scale

当前支持撤销的典型操作：

- 新建 / 删除 Page
- 添加卡片
- 删除卡片
- 移动 / resize 卡片
- 创建连线
- 编组 / 取消编组
- 编辑 Page 名称
- 删除 Page
- 粘贴卡片

注意：

- 不需要 redo。
- 进入加载页、页面切换、点赞等不应强制进入撤销栈，除非用户特别要求。

## 17. 常用开发与验证方式

语法检查：

```powershell
node --check app.js
```

本地启动：

```powershell
node preview-server.js
```

如果需要自动检查页面：

- 可以启动 Chrome remote debugging。
- 用 DevTools Protocol 读取 DOM、样式、坐标。
- 之前已经多次用这种方式验证：
  - Lottie 是否显示。
  - 爱心图标尺寸。
  - 编组边距。
  - 编组拖拽。
  - Page 列表滚动。
  - 光标实际 CSS。

常见检查点：

- 控制台无语法错误。
- `node --check app.js` 通过。
- 创建编组后顶层 DOM 只有外部组，内部卡片是其子节点。
- 拖动编组内部卡片时，拖动过程中父组不要实时改坐标。
- 粘贴卡片时不能弹剪切板读取权限。
- Page 过多时 `.page-list` 应为 `overflow-y: auto`。

## 18. 已踩坑清单

### 字符乱码

项目中曾出现中文 mojibake。继续开发时要尽量使用 UTF-8 读取 / 写入文件。

PowerShell 读取时建议：

```powershell
[Console]::OutputEncoding=[Text.Encoding]::UTF8
Get-Content -Encoding UTF8 app.js
```

### Figma SVG 和图标

用户多次强调图标素材要从 Figma 取，不要用 CSS 或文本随手画，尤其是：

- 点赞爱心。
- 卡片删除 / 适应 / 更多按钮。
- 四角蓝色小环。
- 底部工具栏图标。

### 连接线层级

连接线需要能在编组内可见，不能被编组背景盖住。

当前连接层级高于 `canvasWorld`，但 `pointer-events: none`，避免挡住交互。

如果后续要把连接线拆成组内局部 SVG，记得仍要保证：

- 坐标正确。
- 不挡点击。
- 线宽不随缩放变化。

### 编组自适应

不要在拖动内部卡片时持续自适应编组，否则子卡片移动会忽快忽慢。

正确逻辑：

- 创建编组：做 16px 初始包裹。
- 自动尺寸编组：平时可以校准包裹。
- 拖动内部子卡片：暂停父编组校准。
- 松手后：再校准外框。
- 手动 resize 编组：`autoSize = false`，之后尊重自定义尺寸。

### 剪切板权限

不要在粘贴时调用：

```js
navigator.clipboard.readText()
```

它会弹权限提示，打断操作。

### 光标大小

不要直接引用原始 `.cur`，浏览器会挑大尺寸。

当前用 `assets/cursors-normal/*.png`，固定 `32 x 32`。

### 文字编辑和拖拽冲突

编辑文字时不要触发复制粘贴卡片快捷键。

当前 `isTextEditingTarget()` 用于判断：

```js
[contenteditable="true"], input, textarea
```

复制粘贴卡片快捷键应避开这些目标。

### 图片拖拽

图片和卡片内部元素必须禁用浏览器原生拖拽，否则会把图片 / 文本拖出页面，释放后打开新标签。

相关处理：

- `draggable = false`
- `user-select`
- `-webkit-user-drag: none`

## 19. 当前仍可继续优化的方向

这些不是本会话必须修完的内容，但新窗口可能继续接：

- 继续完善卡片内容真实文案和作品内容。
- 优化编组内部连线在复杂嵌套时的路径遮挡。
- 给复制粘贴增加粘贴到鼠标位置的体验。
- 支持删除连接线。
- 支持 redo。
- 支持连接线选中态。
- 优化移动端或窄屏布局。
- 增加留言弹窗。
- 做云端或本地持久化画布编辑。

## 20. 当前交接结论

这个项目已经不是初始空壳，而是一个可操作的个人画布式网站雏形：

- 加载页、主画布、Page 树、Minimap、工具栏已成型。
- 卡片创建、编辑、移动、resize、连线、编组、复制粘贴、撤销都已具备。
- 视觉资源已经大量对齐 Figma，尤其是点赞、卡片按钮、选中四角、光标、加载页 Lottie。
- 后续开发请优先保持现有交互约定，不要用“简单重写”覆盖掉已经反复调过的细节。
